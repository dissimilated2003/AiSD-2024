------- дура процедура на постройку дерева -------

считать первую строку(линия, файл);
если (не пустая строка) {
    создать узел root со этой строкой;
} иначе {
    завершить программу;
}

while не конец текстового файла {
    если (элемента нет среди сыновей) {
	добавить элемент в вектор сыновей;
	добавить его в ассоциативный массив с ключом отца;
    } иначе если (есть среди сыновей) {
	добавить его в ассоциативный массив с ключом отца;
    }
    если (текущий == отцу предыдущего) {
	удалить из ассоциативного массива(текущий, предыдущий);
    }
    бул PizdaTrassirovke = ложно;
    проверка правильности трассировки(); //возврат правильная трассировка
    если (НЕ правильная трассировка) {
	PizdaTrassirovke = истина;
    }
    если (PizdaTrassirovke) {
	std::cout << "Ошибка трассировки";
	удаление дерева;
	завершение программы;
    } 
}